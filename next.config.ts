import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["api.metaboard.in", "picsum.photos","metaboard-assets.s3.ap-south-1.amazonaws.com"],
  },

};

export default nextConfig;
